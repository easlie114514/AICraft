# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
/dist/
*.egg

# ── 虚拟环境 ──
venv/
.venv/
env/

# ── 运行时数据（用户生成，首次启动自动创建） ──
.version
chroma_db/
memory/
workspace/
# models/onnx 纳入版本管理
config/profiles/
config/app.json
config/permissions.json
config/rag_config.json
rag/sources.json

# ── 前端（dist 纳入版本管理，用户无需 Node.js）──
frontend/node_modules/

# ── IDE ──
.idea/
.vscode/
*.swp
*.swo

# ── 打包产物 ──
/build/
*.spec
!aicraft.spec

# ── 系统文件 ──
.DS_Store
Thumbs.db
desktop.ini
