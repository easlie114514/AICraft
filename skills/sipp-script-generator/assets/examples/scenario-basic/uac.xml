<?xml version="1.0" encoding="UTF-8" ?>
<scenario name="Basic Call UAC Example">
  <!-- 配置说明：
       - field0: 呼叫方号码（例如：1000）
       - service: 被叫方号码（例如：2000）
       - remote_ip: SIP服务器IP（例如：192.168.1.1）
       - remote_port: SIP服务器端口（例如：5060）
       - local_ip: 本地IP地址
       - media_port: 媒体端口（例如：6000）
  -->

  <!-- 发送INVITE -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:[field1]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: [field0] <sip:[field0]@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[field1]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: [cseq] INVITE
      Contact: <sip:[field0]@[local_ip]:[local_port]>
      Max-Forwards: 70
      Subject: Performance Test
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 8 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:8 PCMA/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-15
    ]]>
  </send>

  <!-- 接收100 Trying（背靠背设备必须发送） -->
  <recv response="100">
  </recv>

  <!-- 接收180 Ringing -->
  <recv response="180" rtd="true">
  </recv>

  <!-- 接收200 OK -->
  <recv response="200" rtd="true">
  </recv>

  <!-- 发送ACK -->
  <send>
    <![CDATA[
      ACK sip:[field1]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: [field0] <sip:[field0]@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[field1]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: [cseq] ACK
      Contact: <sip:[field0]@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- 通话保持10秒 -->
  <pause milliseconds="10000"/>

  <!-- 发送BYE -->
  <send retrans="500">
    <![CDATA[
      BYE sip:[field1]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: [field0] <sip:[field0]@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[field1]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: [cseq] BYE
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- 接收200 OK -->
  <recv response="200" rtd="true">
  </recv>
</scenario>
