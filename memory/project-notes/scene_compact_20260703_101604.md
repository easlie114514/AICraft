# 场景记忆归档 20260703_101604
角色: AICraft智能小助手

---

1. 用户核心目标/任务
   - 创建一对SIPP脚本，分别作为主叫侧（核心中继侧）和被叫侧，模拟特定会话场景并进行信令、RTP流和DTMF发送的抓包验证。

2. 已完成的进度和关键决策
   - 创建了目录 `D:\skills`。
   - 创建了两个SIPP脚本文件：`D:\skills\uac.xml`（5 KB）和 `D:\skills\uas.xml`（4 KB）。
   - 脚本覆盖了两个阶段：初始会话（711u+2833，双方发送DTMF和RTP）和主叫侧发送reINVITE协商编解码（转码711u+2833与711a+2833，双方继续发送DTMF和RTP）。

3. 值得跨场景记住的用户偏好
   - 偏好在 `D:\skills` 目录下创建和管理与技术相关的脚本文件。
   - 偏好使用特定目录 `D:\AICraft` 和 `D:\AICraft\workspace` 作为项目的根目录和工作区，并期望在这些路径下拥有直接读写权限。
   - 对系统目录（如 `C:/Windows/**`、`C:/Program Files/**`、`C:/System32/**`）和敏感配置文件（如 `.git/config`、`.ssh/**`）有严格的安全策略限制。

4. 技术细节
   - 目录结构：`roles/`（角色提示词）、`skills/`（技能模块，如代码审查、写作助手）、`config/`（JSON配置）、`models/`（模型配置，含DeepSeek接入设置）、`memory/`（记忆数据和对话历史）、`rag/`（本地知识库）。
   - 读写权限：仅在 `D:\AICraft` 和 `D:\AICraft\workspace` 下具有完整读写权限，其他路径需逐次确认或受安全策略限制。
   - 初始工作区路径：`D:\AICraft\workspace`，曾在此路径下创建过6个 `.txt` 文件，其中最后一个被用户指定到 `D:\AI_Craft\workspace` 路径。